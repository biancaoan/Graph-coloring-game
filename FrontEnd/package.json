{
  "name": "static-web-project",
  "version": "1.0.0",
  "description": "A static web page using Bootstrap, PixiJS, and optional FastAPI backend",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
